# Frontend env (used by Vite at build time)

# Preferred in development: talk to the backend through the Vite proxy
# so both uploads and SSE progress stream are same-origin.
VITE_API_URL=/api

# Optional fallback (e.g., for a static prod build without a reverse proxy):
# If you keep this, App.tsx can read it as a secondary fallback.
VITE_API_BASE_URL=http://127.0.0.1:8002